# GENERATED by `pf bootstrap` (pf.tools.openmetadata). Do not hand-edit:
# it is rewritten from tools.yaml, pf.runtime.targets and the project's
# dbt artefacts.
# Credentials are env-var references — set OPENMETADATA_JWT_TOKEN and the
# warehouse's own variables where this runs. No secret is in this file.
source:
  type: snowflake
  serviceName: zenith_zenith_uk
  serviceConnection:
    config:
      type: Snowflake
      account: ${SNOWFLAKE_ACCOUNT}
      username: ${SNOWFLAKE_USER}
      password: ${SNOWFLAKE_PASSWORD}
      role: ${SNOWFLAKE_ROLE}
      warehouse: ${SNOWFLAKE_WAREHOUSE}
      database: ${SNOWFLAKE_DATABASE}
  sourceConfig:
    config:
      type: DatabaseMetadata
      includeTables: true
      includeViews: true
      markDeletedTables: true
sink:
  type: metadata-rest
  config: {}
workflowConfig:
  openMetadataServerConfig:
    hostPort: http://localhost:8585/api
    authProvider: openmetadata
    securityConfig:
      jwtToken: ${OPENMETADATA_JWT_TOKEN}
