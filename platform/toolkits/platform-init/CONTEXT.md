---
when: A project is empty, or the user says "get started".
rules:
  - Follow the path source → annotate → model → metric; skipping annotation costs that work several times over later.
---

# platform-init

The end-to-end path from a new data source to a queryable metric. Worth following
in order even when a step looks skippable — the annotation step in particular is
what makes everything downstream generate itself.
