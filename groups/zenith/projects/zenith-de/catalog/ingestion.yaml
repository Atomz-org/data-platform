# GENERATED by `pf bootstrap` (pf.tools.openmetadata). Do not hand-edit:
# it is rewritten from tools.yaml and the project's dbt artefacts.
# The JWT is an env-var reference — set OPENMETADATA_JWT_TOKEN where this runs.
source:
  type: dbt
  serviceName: zenith_zenith_de
  sourceConfig:
    config:
      type: DBT
      dbtConfigSource:
        dbtConfigType: local
        dbtManifestFilePath: transform/target/manifest.json
        dbtCatalogFilePath: transform/target/catalog.json
        dbtRunResultsFilePath: transform/target/run_results.json
      dbtUpdateDescriptions: false
      dbtUpdateOwners: true
      includeTags: true
sink:
  type: metadata-rest
  config: {}
workflowConfig:
  openMetadataServerConfig:
    hostPort: http://localhost:8585/api
    authProvider: openmetadata
    securityConfig:
      jwtToken: ${OPENMETADATA_JWT_TOKEN}
