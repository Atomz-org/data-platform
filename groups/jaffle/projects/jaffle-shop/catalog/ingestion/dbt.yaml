# GENERATED by `pf bootstrap` (pf.tools.openmetadata). Do not hand-edit:
# it is rewritten from tools.yaml, pf.runtime.targets and the project's
# dbt artefacts.
# Credentials are env-var references — set OPENMETADATA_JWT_TOKEN and the
# warehouse's own variables where this runs. No secret is in this file.
source:
  type: dbt
  serviceName: jaffle_jaffle_shop
  sourceConfig:
    config:
      type: DBT
      dbtConfigSource:
        dbtConfigType: local
        dbtManifestFilePath: transform/target/manifest.json
        dbtCatalogFilePath: transform/target/catalog.json
        dbtRunResultsFilePath: transform/target/run_results.json
      dbtUpdateDescriptions: false
      dbtUpdateOwners: true
      includeTags: true
sink:
  type: metadata-rest
  config: {}
workflowConfig:
  openMetadataServerConfig:
    hostPort: http://localhost:8585/api
    authProvider: openmetadata
    securityConfig:
      jwtToken: ${OPENMETADATA_JWT_TOKEN}
