{
  "mcpServers": {
    "code-review-graph": {
      "command": "uvx",
      "args": [
        "code-review-graph@2.3.9",
        "serve",
        "--repo",
        "${workspaceFolder}/platform"
      ]
    },
    "graphify": {
      "command": "uvx",
      "args": [
        "--from",
        "graphifyy",
        "graphify-mcp",
        "${workspaceFolder}/graphify-out/graph.json"
      ]
    },
    "pf": {
      "command": "uv",
      "args": [
        "run",
        "pf",
        "mcp"
      ],
      "env": {
        "PF_PROJECT_DIR": "${workspaceFolder}"
      }
    }
  }
}
