# Claude Code, as configured here

The power-user setup for this monorepo: what is wired, where it lives, and which
widely-circulated advice does not apply. Not loaded into any session — read it
when changing the session layer, not while working.

Adapted from [claude-code-for-power-users][guide]. That guide is a good map of
the *shape* of a serious setup; a fair amount of its concrete detail no longer
matches the product. The last section says exactly which parts.

[guide]: https://github.com/PackMaaan/claude-code-for-power-users

---

## Where everything lives

| Layer | Path | Scope |
|---|---|---|
| Router memory | `CLAUDE.md` | repo-wide, always loaded |
| Project memory | `groups/<g>/projects/<p>/CLAUDE.md` + `kg/context_card.md` | one entity, always loaded, **budgeted** |
| Session memory | `.memory/notes/` at root, `platform/`, each group and project · `.memory/MEMORY.md` | per module, on demand, **indexed**, shared with every other agent via `AGENTS.md` |
| Marketplace | `platform/.claude-plugin/marketplace.json` | lists all 19 plugins |
| Toolkits | `platform/toolkits/<name>/` | one craft each, enabled per project |
| Session layer | `platform/toolkits/power-tools/` | commands, agents, hooks, MCP |
| Root settings | `.claude/settings.json` | platform sessions |
| Project settings | `groups/<g>/projects/<p>/.claude/settings.json` | generated by `pf new-project` |
| Policy | `gate.yaml` + `gate.capabilities.yaml` | enforced by two hooks and CI |
| Tests | `platform/tests/<group>/` + `README.md` | grouped by what they guard, **indexed** |
| Container entrypoints | `platform/entrypoints/` | copied in by `platform/Containerfile.*` |

Two directories used to be called `scripts/`, and the name said nothing about
either. `platform/entrypoints/` holds the two container entrypoints; the session
hooks moved in beside the `hooks.json` that calls them, so a hook is one
directory rather than two. `.github/scripts/` keeps its name — that one is
GitHub's convention, not ours.

## Everything a session writes stays in the checkout

> Stated as a control — objective, threat model, evidence, residual risk and
> the one thing still unverified — in
> [`docs/SESSION-CONTAINMENT.md`](SESSION-CONTAINMENT.md). This section is how
> it works; that page is what it guarantees.

A Claude Code session writes in two places that are not the repository: the
Claude Code folder (`~/.claude/projects/<slug>/<session>/`) holds runs,
subagent transcripts and tool results, and a **temp root** holds the session's
scratchpad, background task output and pasted images. On macOS that temp root
defaults to `/private/tmp/claude-<uid>/`.

Both defaults are wrong for this repo. Files there are not findable, not backed
up, not covered by any retention anyone here chose, and they disappear with the
session — which makes them useless as a record of what an agent did.

**The rule, in force order:**

| # | Mechanism | Covers |
|---|---|---|
| 1 | `CLAUDE_CODE_TMPDIR` exported **before the harness starts** — `.envrc`, or `just claude` / `sh bin/claude-here` | the harness writes scratchpad, tasks and images inside the checkout from its first byte |
| 2 | `platform/hooks/session_start.py` symlinks what (1) did not catch | a session started with a plain `claude` is redirected anyway |
| 3 | the SessionStart context line | tells the agent to put its own scratch files under `.tmp/` |
| 4 | `pf workflow capture` | copies what cannot safely be linked — see below |

**(1) is prevention; the rest is recovery.** Redirecting a directory after the
harness has made it has a window before the hook runs, races a session that is
already writing, and cannot move `tasks/` at all once the harness has looked at
it. When (1) is in force `pf workflow link --check` reports each scratch kind as
`linked: the harness writes here directly`, and no symlink is created.

### Why it cannot be a setting

This is the part that surprises people, and it cost a session to learn:

> **`.claude/settings.json` cannot do it.** Claude Code resolves its temp root
> once, at process start, from the environment it inherits. An `env` block in
> project settings is applied to the tool and hook subprocesses the harness
> spawns *afterwards* — by then the session folder has already been placed. The
> variable has to be exported by whatever launches `claude`.

The harness says so itself in the error it prints when that folder moves under
it: *"restart Claude Code with CLAUDE_CODE_TMPDIR set"* — set by the launching
shell, not by the project it is about to open.

`CLAUDE_CODE_TMPDIR` is still declared in `.claude/settings.json` so that tools
and hooks agree with the harness when it *is* set. `workflows._literal()`
discards a value still containing `$`: an unexpanded `${CLAUDE_PROJECT_DIR}`
would otherwise resolve against the working directory, invent a root named after
the variable, and rank it ahead of the one actually in use.

### Why the Claude Code folder is copied, not linked

Claude Code's retention sweep walks `subagents`, `workflows` and `remote-agents`
under a session folder and deletes anything older than `cleanupPeriodDays` (30
by default). It recurses with `readdir`, which **follows a symlinked directory**
— so linking one of those three into the repo would put repo history behind a
30-day delete. Leaf entries are safe (the sweep lstats them and at most unlinks
the link), which is why `subagents/workflows` and `workflows/scripts` are linked
and their parents are not. Everything else there is copied by
`pf workflow capture`, and the harness keeps its own.

### The one thing a hook will not do

`tasks/` is never adopted from a running session. Claude Code notes that
directory at startup and checks it on every tool call; finding it moved or
replaced by a link it refuses to swap the output file into place, and **no
command's output reaches the model again until the session restarts**. Creating
and linking it before the harness first looks is fine and is what a new session
does. Replacing a populated one underneath a live process is not, so that waits
for `pf workflow link --adopt` between sessions
(`workflows.ADOPT_NOT_WHILE_LIVE`).

### Checking it

```bash
echo "$CLAUDE_CODE_TMPDIR"          # should be <repo>/.tmp, fully expanded
uv run pf workflow link --check     # every session, every kind, where it points
```

Three outcomes for the second command, per kind:

| Reported | Means |
|---|---|
| `linked: the harness writes here directly` | (1) is in force — nothing to redirect |
| `linked -> .tmp/<session>/<kind>` | (2) caught it; the bytes are in the repo, the path is not |
| `refused: …` | that session is writing outside the repo, and the line says why |

A kind reported `refused` means that session is writing outside the repo and
the line says why. `.tmp/` is gitignored — and `git clean -xdf` takes it, along
with any live session's state. That is the trade for having it here.

## Finding a test without reading the suite

`platform/tests/` is grouped by the part of the platform each file guards —
`ontology/`, `graph/`, `capabilities/`, `onboarding/`, `gate/` — so a whole area
runs on its own with `uv run pytest platform/tests/<group>`.

`platform/tests/README.md` is the index, generated from the suite's own
docstrings. Reading it costs ~800 tokens against ~4,000 lines of test code, and
it cannot drift: `pf test check` fails when it and the suite disagree, and
`gate/test_suite_index.py` runs that check inside the suite itself.

```bash
uv run pf test where policy        # which file guards this?
uv run pf test where pf.kg.build   # which tests import this module?
uv run pf test index               # regenerate after adding a test file
```
| Scaffolding | `platform/src/pf/scaffold/` + `platform-init/scaffold-project` | plan → apply → verify ([docs](SCAFFOLDING.md)) |

## Creating a project without exploring the repo

A platform session that scaffolds a project should cost about a hundred tokens
of decision, not eight thousand of discovery. `pf new-project --plan` resolves
the capability set, the gate rules, the CI jobs and the blockers in one call and
writes nothing; the `scaffold-project` skill carries the sequence and the exit
criteria. Neither requires reading `pf/capabilities.py`, the generator, or the
tree the scaffolder just created — `kg/context_card.md` is the index, and it is
generated.

```bash
uv run pf new-project <g> <p> --plan   # ~124 tokens, exits 1 if blocked
uv run pf new-project <g> <p>          # apply; runs the whole bootstrap
uv run pf check                        # the third exit criterion
```

Two commands cover normal use:

```bash
uv run pf work <group> <project>   # a session scoped to one entity
uv run pf --help                   # everything else
```

`pf work` chdirs into the project and execs `claude`, which is what makes the
project's settings, plugins, MCP server and card the ones that load. Running
`claude` from the repo root instead gives you a *platform* session — different
scope, different permissions, no warehouse tools.

---

## 1. Memory: CLAUDE.md

The guide's advice is "put everything in CLAUDE.md". Here that is wrong, and the
repo enforces the opposite: `pf tokens` fails the build when a card or project
`CLAUDE.md` exceeds its budget, because always-on context is paid by every
request for the life of the project.

The structure instead:

- **Always on, budgeted** — the router (`CLAUDE.md`), the project card
  (`kg/context_card.md`, generated by `pf kg card`), the group card and the
  group `CLAUDE.md`. Facts that every turn needs.
- **On demand** — skills in the toolkits. Loaded when their description matches
  what you are doing, free otherwise.
- **On demand, shared by sisters**: the group's skills in
  `groups/<g>/.claude/skills/`, loaded through the group marketplace. The
  manifest at `groups/<g>/.claude/.claude-plugin/plugin.json` is what makes
  that directory a plugin; without it the marketplace entry loads nothing, and
  `pf loop audit` reports it ("group plugins resolve").
- **Queried** — two graphs, neither ever loaded. The knowledge graph answers
  models, columns, metrics and lineage (`pf kg`, `kg_search`,
  `impact_analysis`). The code graph answers which Python function calls
  which, under `platform/` only (`pf code`, and its own MCP server). Asking
  the wrong one gets a confident answer to a different question.
- **Remembered, per module** — `.memory/notes/` at the root, under
  `platform/`, and under each group and project: one lesson per file, the
  traps that already cost a session. The SessionStart hook prints the ones
  visible from where you are (root and platform always, your group and project
  inside one — never a sister), one line each and capped; `pf memory show`
  reads the bodies. `pf memory add <module> <name> "<line>"` writes one and
  regenerates the index `.memory/MEMORY.md`, which `pf memory check` gates in
  CI. It lives in the repo rather than in `~/.claude` so that every other
  agent — whatever reads `AGENTS.md`, `GEMINI.md` or the Copilot
  instructions — sees exactly the same notes, and each note records which
  tool wrote it.

So a new fact goes in the layer that matches how often it is needed. Business
rules the graph cannot encode go in the project `CLAUDE.md` under that heading.
Anything derivable from the models belongs in the graph, not in prose.
Loop agents read the same rules: the group and project CLAUDE.md rules are part
of the cached prefix `pf.agents.base.cached_prefix` builds, so a rule written
for a session binds the loops too.

```bash
uv run pf tokens          # the budget, enforced
uv run pf kg card <g> <p> # regenerate a card rather than hand-trimming it
```

## 2. Context management

The guide's answer to a large codebase is scope flags. Those flags do not exist.
The real levers here, cheapest first:

| Instead of | Do |
|---|---|
| reading a project's tree to orient | `kg/architecture.md` — every feature, ~2k tokens |
| reading models to find a dependency | `kg_neighbors`, `kg_path` |
| grepping for where a concept is used | `kg_search` |
| reading downstream models to judge a change | `impact_analysis` |
| loading a whole project | `/gemini-analyze`, scoped to one project dir |
| carrying an old task's context | `/clear` |

`/context-budget` reports where the session's always-on context is going.

**One entity per session.** Never read another group or sister project. Business
logic does not transfer between entities, so an assumption carried across is a
bug — and the settings deny the read, in both directions.

## 3. Tools: the MCP server

`platform/toolkits/power-tools/.mcp.json` starts `pf mcp` as the `pf` server.
23 tools, scoped to one project by `PF_PROJECT_DIR` (falling back to cwd):

- **graph** — `kg_search`, `kg_neighbors`, `kg_path`, `impact_analysis`
- **warehouse** — `list_tables`, `preview_table`, `execute_sql_query`,
  `display_schema`, `get_row_counts`
- **dbt** — `dbt_list`, `dbt_build`, `dbt_test`, `get_model_details`
- **metrics** — `list_metrics`, `get_dimensions`, `query_metrics`
- **ontology** — `ontology_classes`, `validate_annotations`
- **pipelines / secrets** — `get_local_pipeline_state`, `secrets_view_redacted`,
  `secrets_update_fragment`
- **toolkits** — `list_toolkits`, `toolkit_info`

Every data-returning tool truncates: schema, ≤20 rows, summary stats. Never a raw
dump. That policy is the cheapest defence against a context blowup, and it lives
in the server rather than in an instruction anyone can forget.

In a **root session** there is no active project, so warehouse and graph tools
answer "not inside a project". That is the isolation working.

```bash
uv run pf mcp        # run it by hand
just mcp
```

## 4. Commands

In `platform/toolkits/power-tools/commands/`. The guide's six, plus three this
platform can actually answer.

| Command | Does |
|---|---|
| `/security-audit` | secrets at rest, gate holes, PII in committed recce artefacts |
| `/performance-audit` | dbt timing from `run_results.json`, DuckDB plans, dlt throughput |
| `/architecture-review` | ontology conformance, declared joins, grain, layering |
| `/tech-debt` | inert governance first, then model/source/doc debt |
| `/blast-radius` | what breaks if a node changes — run **before** editing |
| `/ship` | gate → check → build → recce → one conventional commit |
| `/context-budget` | where always-on context goes, and how to get it back |
| `/gemini-analyze` | hand a repo-sized question to a bigger window |
| `/ast-grep` | structural search (requires `brew install ast-grep`) |

Each keeps the guide's generic checklist at the bottom, for the cases the
platform-specific procedure does not cover.

## 5. Subagents

In `platform/toolkits/power-tools/agents/`. All four are **read-only** — they
declare `disallowedTools: Write, Edit, MultiEdit, NotebookEdit`. A verifier that
can edit stops being a second opinion and becomes a second author.

| Agent | Catches |
|---|---|
| `impact-verifier` | downstream that *silently changes value*, which a dependency list does not distinguish from "recompiles" |
| `sql-reviewer` | grain violations, fanout, null-swallowing joins, wrong `unique_key` |
| `secrets-auditor` | credentials and PII in tracked files, history, and committed artefacts |
| `semantic-conformance` | broken links in ontology → annotation → model → metric → contract |

Invoke by asking for one by name, or let the model pick from the description.

## 6. Hooks

Four events, configured in `platform/toolkits/power-tools/hooks/hooks.json` plus
the pre-existing gate in the root `.claude/settings.json`. Two scripts on the
same event both run.

| Event | Script | Effect |
|---|---|---|
| `PreToolUse` | `platform/hooks/pre_tool_use.py` | **blocks** writes to generated artefacts, secrets and shared infra; prints blast radius for models and sources |
| `PostToolUse` | `hooks/format_edit.py` | `ruff format` + import sort on `.py`; `sqlfluff fix` on dbt `.sql` |
| `SessionStart` | `hooks/session_start.sh` | injects branch, active entity, and whether the graph is present/stale |
| `Stop`, `Notification` | `hooks/notify.sh` | desktop notification + spoken line + append-only log |
| `SessionStart` | `platform/hooks/session_start.py` | points this session's workflow directories at `logs/workflows/`, so runs are written inside the repo rather than under `~/.claude` (section 10) |

Two design rules worth keeping if you edit them:

- **The formatter never removes code.** Autofix is restricted to import sorting
  (`--select I`). A bare `ruff check --fix-only` deletes unused imports, and an
  import added moments before the code that uses it is legitimately unused. A
  hook that removes what the agent is about to use is worse than no hook.
- **SQL that will not parse is skipped, not half-formatted.** Models using
  project macros template to an unparsable tree under the stubbed `jinja`
  templater; sqlfluff refuses to rewrite those, and the hook stays quiet.

Notifications, off switches:

```bash
PF_NOTIFY=0        # silent
PF_NOTIFY_VOICE=0  # desktop only
PF_NOTIFY_LOG=...  # default ~/.claude/pf-task-log.txt
```

Set in `.claude/settings.json` under `env`.

## 7. Permissions

`permissions` in `.claude/settings.json`, at root and per project. Not
`.claude/permissions.json` — that file does not exist in any current version.

- **deny** — reading `.env*`, `**/secrets.toml`, `**/credentials/**`; writing
  `vendor/**`; and per project, reading any sister or other group.
- **ask** — `git push`, `gh pr create`, `gh pr merge`, submodule updates.
- **allow** — the `pf`, `dbt`, `mf` and read-only `gh` verbs used constantly.

Permissions and the gate are different mechanisms and both are needed:
permissions decide whether a *tool call* happens, `gate.yaml` decides whether a
*path* may change, and the gate also runs in pre-commit and CI where no agent is
involved.

## 8. Enforcement outside the session

```bash
just hooks                 # install .git/hooks/pre-commit (not cloned with the repo)
uv run pf gate --paths a,b # the same policy, by hand
uv run pf check            # conformance + blast radius of your changes
uv run pf loop audit       # scored readiness: gate, hooks, marketplace, MCP, graphs
uv run pf loop list --group <g>   # the registry with the group's loops.yaml applied
```

`pf loop audit` is the one to run after changing anything in this document's
scope. It scores the session layer, including two checks that exist because this
exact configuration silently broke once: **plugin marketplace resolves** and
**MCP tools available to projects**. A third, **group plugins resolve**, applies
the marketplace rule to every `groups/<g>/.claude-plugin/marketplace.json`.
`pf loop list --group <g>` shows which loops watch a family once its
`loops.yaml` is applied, waivers and disabled loops included.

CI is **one workflow per project**, named after it: `.github/workflows/<project>.yml`
holds every job that project runs (`impact-gate`, `recce`, and whatever a future
capability adds). It is generated by `pf bootstrap` from the `ci_jobs` each
capability declares — do not hand-edit it; change the capability. A `changes` job
diffs the PR once and publishes a boolean per area, and every job guards on it,
so editing a page never starts a dbt review.

Three of those jobs judge the *repository*, not the runner, and it pays to know
how. `architecture` re-renders `kg/architecture.md` from tracked and staged
files only — run `git add` before `pf arch`, or an untracked note makes your
render disagree with CI's; artefacts regenerated per machine (the context card,
`platform/workspace.yaml`) show as `⟳` rows. `impact-gate` restores the group
from the base under the PR's own `platform/` and blocks on a radius that
reaches a metric or an exposure — unless the same PR adds or edits a decision
record under `decisions/`, in which case it reports and passes: the radius and
its owners are still printed, and the ADR is what the reviewer reads. `recce`
needs the branch to contain its base, and says so in a PR comment.

Repo-wide workflows sit alongside: `pr-report.yml` (impact report on every PR),
`vendor-sync.yml`, and `claude-review.yml` — the last is opt-in, triggered by
adding the `claude-review` label, because it is billed per run.

## 9. Extended thinking

`think` → `think hard` → `ultrathink`, in the prompt. Worth reaching for on
ontology changes, anything touching a published contract, and any change whose
blast radius crosses into metrics. Not worth it for a staging model.

`MAX_THINKING_TOKENS` is deliberately **not** set here. It is a *cap*, not a
boost — setting it to the number the guide suggests can lower the budget rather
than raise it, depending on model and effort level. Leave it unset unless you are
deliberately constraining spend.

## 10. Workflow runs

The Workflow tool splits a run across two directories under
`~/.claude/projects/<slug>/<session>/`: `subagents/workflows/<run-id>/` holds the
journal and every subagent transcript, and `workflows/` holds `<run-id>.json`
(the launch record, with the script text and the task id) and
`scripts/<name>-<run-id>.js`. Both are keyed by session id, so a `/clear` or a
new terminal hides the lot.

`pf workflow link` replaces `subagents/workflows` and `workflows/scripts` with
symlinks to `logs/workflows/`, so the harness writes runs **inside the repo as
they happen**. Nothing is copied and nothing is moved afterwards: the file an
agent is appending to is the file in the repo, which is why progress is visible
while the run is going. Git ignores `logs/`, so nothing lands in a commit.

```
logs/workflows/
  README.md                          written by `pf workflow link`
  wf_f00f2249-53e/journal.jsonl      launched, started, result, failed
  wf_f00f2249-53e/agent-<id>.jsonl   one subagent's transcript
  scripts/<name>-wf_f00f2249-53e.js  the script; resume with resumeFromRunId
```

**`workflows/` itself is deliberately not linked**, and its `<run-id>.json`
launch record stays under `~/.claude`. For a session whose transcript it has
already removed, Claude Code sweeps three directories:

```js
for (const c of ["subagents", "workflows", "remote-agents"])
    if (await isDir(join(dir, c))) await purge(join(dir, c), cutoff)
```

`purge` recurses with `readdir`, deletes every file older than
`cleanupPeriodDays` (30 by default) and prunes the directories it empties.
`readdir` follows a symlinked directory, so following one of those three paths
into the repo would delete run history a month later.

Today it would not get that far: `isDir` is `lstat(p).isDirectory()`, false for a
symlink, so a link at `workflows/` is skipped rather than entered (checked in
2.1.250, 2.1.261, 2.1.263). That is a safe accident, not a guarantee — `lstat`
rather than `stat`, one word in a minified bundle. Linking a *leaf entry* needs
no such luck: `purge` reaches an entry that is neither directory nor file through
a branch that lstats it and at most unlinks the link, leaving the target alone.
So the two links that exist are safe either way, the next session's hook puts
back any link the sweep removes, and the launch record — which only duplicates
the script text — is left where it falls.

Raising `cleanupPeriodDays` here would not be an alternative: the sweep walks
every project under `~/.claude/projects` using the settings of whichever session
is running, so a session started in another repo sweeps this one with that
repo's cutoff.

```bash
uv run pf workflow link [--adopt]   # point this repo's sessions at logs/workflows/
uv run pf workflow link --check     # report only; exit 1 if a session is unlinked
uv run pf workflow list             # every run, newest first; live or complete
uv run pf workflow watch            # live progress; Ctrl-C to stop
uv run pf workflow show <run-id>    # phases and agents of one run
uv run pf workflow sync [--move]    # adopt runs written before the link existed
```

`sync` is the fallback, not the strategy: it copies a run a session wrote while
unlinked, verifies it by SHA-256 and only then removes the session copy. A run
counts as complete when every started agent has a result or failed record and
nothing in its folder changed for 120 s; there is no end-of-run marker, and
between two phases a live run briefly looks finished, which is what the quiet
period is for. `--adopt` on `link` runs the same path for a session that already
holds runs, and refuses while any of them is live.

Two refusals worth knowing. A link that points the other way (`logs/workflows`
itself linked out to a session folder) does not count as being in the repo:
the bytes are still under `~/.claude`, and `list` says `session`. Anything in a
session directory that is not a run, a launch record or a script is refused
rather than moved, because the alternative is deciding on someone's behalf that
a file in their session folder is disposable.

---

## What in the source guide does not work

Following these verbatim wastes time. Current as of Claude Code 2.1.x.

| Guide says | Reality |
|---|---|
| `.claude/permissions.json` | No such file. `permissions` lives in `.claude/settings.json`. |
| `claude --scope=` / `--exclude=` / `--include=` | No such flags. Scope by cwd (`pf work`), and by asking the graph instead of reading files. |
| `claude --max-context=` / `--clear-context` | No such flags. `/clear`, and `autoCompactWindow` in settings. |
| `/clear --conversation-only`, `--rescan-project`, `--except=` | `/clear` takes no arguments. |
| `claude --session=` / `--resume=<name>` | `--resume` takes a session id, not a name; `-c` continues the last one. |
| `claude --confirmation-required` | Not a flag. Use `permissions.ask`. |
| `claude auth login` | Not a subcommand. `/login` in-session. |
| `config.json` with `temperature`, `thinking_budget`, `allowedTools` | Not the schema. Settings keys are documented under `.claude/settings.json`. |
| `DISABLE_TELEMETRY`, `DISABLE_ERROR_REPORTING` | Not current names. Telemetry is opt-**in** via `CLAUDE_CODE_ENABLE_TELEMETRY`. |
| `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC`, `DISABLE_NON_ESSENTIAL_MODEL_CALLS`, `ENABLE_BACKGROUND_TASKS`, `FORCE_AUTO_BACKGROUND_TASKS` | Not in the documented environment set. |
| `CLAUDE_CODE_CONTAINER_MODE`, `BYPASS_ALL_CONFIRMATIONS` | Invented. `--dangerously-skip-permissions` is the real thing, and is for disposable containers only. |
| `claude mcp restart --all` | Not a subcommand. `/mcp` in-session. |
| `/commands` to list custom commands | `/help`, or `claude plugin details <plugin>`. |
| Marketplace `source` paths relative to `marketplace.json` | They resolve from the **marketplace root** — the directory holding `.claude-plugin/`. Getting this wrong silently loads zero plugins, which is what happened here. |

What the guide gets right, and is worth keeping: CLAUDE.md as the highest-leverage
file, custom commands as the way to encode a team's process, hooks as the way to
make a rule fire without an agent remembering it, subagents for verification, and
"plan before implementing" as the default posture.
