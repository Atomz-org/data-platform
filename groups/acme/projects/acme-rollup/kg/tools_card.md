<!-- GENERATED by `pf context build`. Edit the Capability declaration,
     never this file. -->
# Capabilities — acme/acme-rollup

None enabled. `pf capabilities` lists what is available; add one with
`pf capability-add <name> acme acme-rollup`.
