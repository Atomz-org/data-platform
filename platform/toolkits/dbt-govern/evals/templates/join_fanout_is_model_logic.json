{
  "template": "join_fanout_is_model_logic",
  "agent": "test_failure_triage",
  "why": "The inverse of the stale-source error: real data, current freshness, and a uniqueness test failing because a join multiplied rows. The distinguishing evidence is a row count that grew while the source's did not, plus a recent change to the model's own SQL. An agent that blames the source here sends someone to re-run a pipeline that is working perfectly.",
  "tags": ["triage", "classification"],
  "requires": {"mart": "mart"},
  "input": {
    "failures": [
      {
        "unique_id": "test.unique_{{mart.name}}_{{mart.column}}",
        "status": "fail",
        "message": "Got 41,882 results, configured to fail if != 0. Duplicate {{mart.column}} values detected."
      }
    ],
    "lineage": "model:{{mart.name}} [grain: {{mart.grain}}] — 79,986 rows out; previous successful build produced 38,104 rows; sql modified 3 hours ago\n  columns: {{mart.columns}}\n\nAll upstream sources loaded on schedule within the last 2 hours, with unchanged row counts and no schema changes.\n\nRecent git activity: {{mart.name}}.sql was modified 3 hours ago — a LEFT JOIN to an upstream table was added in that commit. That table holds many rows per key of {{mart.name}}'s declared grain."
  },
  "expect": {
    "root_cause": "model_logic",
    "confidence": {"one_of": ["medium", "high"]},
    "evidence": {"contains_any": ["join", "row", "duplicate", "fan", "grain"]}
  }
}
