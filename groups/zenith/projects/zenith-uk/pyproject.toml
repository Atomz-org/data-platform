[project]
name = "zenith_uk"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = ["pf"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/zenith_uk"]
