# GENERATED by `pf bootstrap`. Do not edit.
#
# Every check below is derived, not written:
#
#   contracts/annotations.yaml   column -> ontology role
#   ontology concepts.yaml       role   -> review intent, pii flag
#   pf.tools.recce               intent -> check type
#
# So the way to change a check is to fix what the column *means*, not
# to edit here: a wrong check is a wrong role. Columns whose role is
# PII are excluded from every value-level check, because this file's
# results land in recce_state.json, which is durable and shared.
#
# Regenerate with `pf tool recce config <group> <project>`.
checks:
- name: Row count diff
  description: Row counts for every modified table model.
  type: row_count_diff
  params:
    select: state:modified,config.materialized:table
- name: Schema diff
  description: Column additions, removals and type changes.
  type: schema_diff
- name: "Value diff \u2014 dim_customers"
  description: Row-level differences in dim_customers (one customer), keyed on customer_id.
  type: value_diff
  params:
    model: dim_customers
    primary_key: customer_id
- name: "Profile diff \u2014 dim_customers"
  description: Distribution of subscription_count, active_subscriptions (one customer).
    A shifted total is the failure lineage cannot predict.
  type: profile_diff
  params:
    model: dim_customers
    columns:
    - subscription_count
    - active_subscriptions
- name: "Category drift \u2014 dim_customers.customer_segment"
  description: Values gained or lost in customer_segment (one customer). A category
    that appears or disappears breaks an accepted_values test downstream without moving
    the row count.
  type: query_diff
  params:
    sql_template: select customer_segment as category, count(*) as n from {{ ref('dim_customers')
      }} group by 1 order by 1
    primary_keys:
    - category
- name: "Category drift \u2014 dim_customers.country_code"
  description: Values gained or lost in country_code (one customer). A category that
    appears or disappears breaks an accepted_values test downstream without moving
    the row count.
  type: query_diff
  params:
    sql_template: select country_code as category, count(*) as n from {{ ref('dim_customers')
      }} group by 1 order by 1
    primary_keys:
    - category
- name: "Value diff \u2014 fct_payments"
  description: Row-level differences in fct_payments (one payment attempt), keyed
    on payment_id.
  type: value_diff
  params:
    model: fct_payments
    primary_key: payment_id
- name: "Profile diff \u2014 fct_payments"
  description: Distribution of amount (one payment attempt). A shifted total is the
    failure lineage cannot predict.
  type: profile_diff
  params:
    model: fct_payments
    columns:
    - amount
- name: "Category drift \u2014 fct_payments.currency_code"
  description: Values gained or lost in currency_code (one payment attempt). A category
    that appears or disappears breaks an accepted_values test downstream without moving
    the row count.
  type: query_diff
  params:
    sql_template: select currency_code as category, count(*) as n from {{ ref('fct_payments')
      }} group by 1 order by 1
    primary_keys:
    - category
- name: "Category drift \u2014 fct_payments.payment_status"
  description: Values gained or lost in payment_status (one payment attempt). A category
    that appears or disappears breaks an accepted_values test downstream without moving
    the row count.
  type: query_diff
  params:
    sql_template: select payment_status as category, count(*) as n from {{ ref('fct_payments')
      }} group by 1 order by 1
    primary_keys:
    - category
- name: "Category drift \u2014 fct_payments.customer_segment"
  description: Values gained or lost in customer_segment (one payment attempt). A
    category that appears or disappears breaks an accepted_values test downstream
    without moving the row count.
  type: query_diff
  params:
    sql_template: select customer_segment as category, count(*) as n from {{ ref('fct_payments')
      }} group by 1 order by 1
    primary_keys:
    - category
- name: "Category drift \u2014 fct_payments.country_code"
  description: Values gained or lost in country_code (one payment attempt). A category
    that appears or disappears breaks an accepted_values test downstream without moving
    the row count.
  type: query_diff
  params:
    sql_template: select country_code as category, count(*) as n from {{ ref('fct_payments')
      }} group by 1 order by 1
    primary_keys:
    - category
- name: "Category drift \u2014 fct_payments.plan_tier"
  description: Values gained or lost in plan_tier (one payment attempt). A category
    that appears or disappears breaks an accepted_values test downstream without moving
    the row count.
  type: query_diff
  params:
    sql_template: select plan_tier as category, count(*) as n from {{ ref('fct_payments')
      }} group by 1 order by 1
    primary_keys:
    - category
- name: "Profile diff \u2014 fct_revenue"
  description: Distribution of net_amount, gross_amount, payment_count (one day x
    segment x plan). A shifted total is the failure lineage cannot predict.
  type: profile_diff
  params:
    model: fct_revenue
    columns:
    - net_amount
    - gross_amount
    - payment_count
- name: "Category drift \u2014 fct_revenue.currency_code"
  description: Values gained or lost in currency_code (one day x segment x plan).
    A category that appears or disappears breaks an accepted_values test downstream
    without moving the row count.
  type: query_diff
  params:
    sql_template: select currency_code as category, count(*) as n from {{ ref('fct_revenue')
      }} group by 1 order by 1
    primary_keys:
    - category
- name: "Category drift \u2014 fct_revenue.customer_segment"
  description: Values gained or lost in customer_segment (one day x segment x plan).
    A category that appears or disappears breaks an accepted_values test downstream
    without moving the row count.
  type: query_diff
  params:
    sql_template: select customer_segment as category, count(*) as n from {{ ref('fct_revenue')
      }} group by 1 order by 1
    primary_keys:
    - category
- name: "Category drift \u2014 fct_revenue.plan_tier"
  description: Values gained or lost in plan_tier (one day x segment x plan). A category
    that appears or disappears breaks an accepted_values test downstream without moving
    the row count.
  type: query_diff
  params:
    sql_template: select plan_tier as category, count(*) as n from {{ ref('fct_revenue')
      }} group by 1 order by 1
    primary_keys:
    - category
