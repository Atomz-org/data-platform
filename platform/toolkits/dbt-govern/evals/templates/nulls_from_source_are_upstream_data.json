{
  "template": "nulls_from_source_are_upstream_data",
  "agent": "test_failure_triage",
  "why": "Separates `upstream_data` from `model_logic` where both would produce the same failing test. What settles it is that the nulls are already present at the source, in a column the model only passes through. Getting this wrong means rewriting a correct model to paper over a genuine ingestion defect — and the defect keeps landing.",
  "tags": ["triage", "classification"],
  "requires": {"src": "source", "stg": "staging_model"},
  "input": {
    "failures": [
      {
        "unique_id": "test.not_null_{{stg.name}}_{{stg.column}}",
        "status": "fail",
        "message": "Got 8,431 results, configured to fail if != 0. Null values in {{stg.column}}."
      }
    ],
    "lineage": "source:{{src.name}} (loaded 1h ago, on schedule)\n  Column profile at load time for the field feeding {{stg.column}}: 8,431 nulls / 204,551 rows (4.1%). Previous load: 0 nulls / 203,880 rows.\n  -> model:{{stg.name}} (sql unchanged for 61 days; {{stg.column}} is selected directly with no transformation, no filter and no coalesce)\n\nRecent git activity on this lineage: none in the last 30 days.\nUpstream note: the operational system behind {{src.name}} deployed a change 26 hours ago that made this field optional at entry."
  },
  "expect": {
    "root_cause": "upstream_data",
    "confidence": {"one_of": ["medium", "high"]},
    "evidence": {"contains_any": ["source", "null", "upstream", "optional", "load"]}
  }
}
