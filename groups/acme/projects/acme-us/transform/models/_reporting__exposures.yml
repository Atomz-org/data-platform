# Generated by `pf report build` — do not edit.
#
# One exposure per Evidence page, so `pf impact` on a column or a model
# reaches the dashboards that read it and names who to tell. Deleting a
# page removes its exposure on the next build; editing this file by hand
# is overwritten.
# group: acme  project: acme-us
version: 2

exposures:
  - name: report_index
    label: "acme-us — Overview"
    type: dashboard
    maturity: high
    url: reporting/pages/index.md
    description: >
      Evidence page generated by `pf report build`. Edit the metric or the
      page, never this file.
    depends_on:
      - ref('fct_payments')
    owner:
      name: "Data Platform"
      email: "data-platform@example.com"

  - name: report_metrics_active_customers
    label: "Active Customers"
    type: dashboard
    maturity: high
    url: reporting/pages/metrics/active_customers.md
    description: >
      Evidence page generated by `pf report build`. Edit the metric or the
      page, never this file.
    depends_on:
      - ref('dim_customers')
    owner:
      name: "Data Platform"
      email: "data-platform@example.com"

  - name: report_metrics_aov
    label: "Average Order Value"
    type: dashboard
    maturity: high
    url: reporting/pages/metrics/aov.md
    description: >
      Evidence page generated by `pf report build`. Edit the metric or the
      page, never this file.
    depends_on:
      - ref('fct_payments')
    owner:
      name: "Data Platform"
      email: "data-platform@example.com"

  - name: report_metrics_gross_payment_volume
    label: "Gross Payment Volume"
    type: dashboard
    maturity: high
    url: reporting/pages/metrics/gross_payment_volume.md
    description: >
      Evidence page generated by `pf report build`. Edit the metric or the
      page, never this file.
    depends_on:
      - ref('fct_payments')
    owner:
      name: "Data Platform"
      email: "data-platform@example.com"

  - name: report_metrics_payment_count
    label: "Payments"
    type: dashboard
    maturity: high
    url: reporting/pages/metrics/payment_count.md
    description: >
      Evidence page generated by `pf report build`. Edit the metric or the
      page, never this file.
    depends_on:
      - ref('fct_payments')
    owner:
      name: "Data Platform"
      email: "data-platform@example.com"

  - name: report_metrics_revenue
    label: "Revenue"
    type: dashboard
    maturity: high
    url: reporting/pages/metrics/revenue.md
    description: >
      Evidence page generated by `pf report build`. Edit the metric or the
      page, never this file.
    depends_on:
      - ref('fct_payments')
    owner:
      name: "Data Platform"
      email: "data-platform@example.com"
