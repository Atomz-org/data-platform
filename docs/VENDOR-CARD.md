# Vendored upstreams — index

Generated by `pf vendor card`. Detail: `docs/VENDOR.md` · `pf vendor why <file>` · `pf vendor list -v`.

| upstream | for | coupling | state |
|---|---|---|---|
| `openmetadata-standards` | entity schemas our catalogue payloads validate against | schema | ok |
| `opentopology` | the traceability chain: intent → constraint → artifact → evidence | schema, shape | ok |
| `recce-pr-update` | Composite action keeping a PR branch merged with its base | data | ok |
| `wrenai` | MDL — the interchange format BI and text-to-SQL consume | schema | ok |
| `dagster-skills` | asset-first orchestration; @dbt_assets is what makes lineage cross dlt/dbt | port | ok |
| `dbt-agent-skills` | how an agent drives dbt — rewritten for dbt Core + DuckDB | port | ok |
| `dlthub-ai-workbench` | toolkit/skill layout, and the annotate-then-generate ingest flow | port, shape | ok · licence? |
| `duckdb-skills` | driving DuckDB from an agent, read-only and truncated by policy | port | ok |
| `recce-claude-plugin` | Recce's own agent skills, agents and MCP wiring | port, shape | ok |
| `loop-engineering` | loop governance: state, gate, budget, circuit breaker, autonomy ladder | parity, shape | ok |
| `ai-governance-framework` | the risk taxonomy and control catalogue, cross-walked to the regulators | data, parity, shape | ok · licence? |
| `asqav-compliance` | the five governance controls a CI scan checks agent code for | data, shape | unreviewed · licence? |
| `context-ontology-accelerator` | Scan → Model → Serve induction, with a steward in the middle | shape | ok |
| `evidence-bi` | BI as a projection of the semantic layer, not a second place metrics live | shape | ok · licence? |
| `ontology-playground` | what a business ontology actually looks like, and RDF/XML fidelity | shape | ok |
| `public-sector-ai-playbook` | the governance artefacts a regulated deployment has to produce | shape | unreviewed · licence? |
| `recce` | dbt PR review: the empirical half of impact analysis | data, shape | ok |
| `recce-ui` | React component library behind the Recce review UI | parity | ok |
| `shadcn-admin` | dashboard shell and theme presets, reference only | shape | ok |
| `forge` | the control plane's component layer and design tokens | data, shape | ok |
| `openmetadata` | catalogue ingestion, and the dbt connector for lineage | shape | ok |

**Contracts that can fail a build** — our output is validated against the vendored file, not against memory:

- `openmetadata-standards` → `schemas/api/data/createGlossary.json`
- `openmetadata-standards` → `schemas/api/data/createGlossaryTerm.json`
- `openmetadata-standards` → `schemas/api/classification/createClassification.json`
- `openmetadata-standards` → `schemas/api/classification/createTag.json`
- `openmetadata-standards` → `schemas/api/data/createMetric.json`
- `openmetadata-standards` → `schemas/type/basic.json`
- `opentopology` → `schemas/otop-core-v0.2.schema.json`
- `wrenai` → `core/wren-mdl/mdl.schema.json`
- `loop-engineering` → `patterns/registry.schema.json`
- `ai-governance-framework` → `docs/_data/ai_deployment_model.yml`
- `ai-governance-framework` → `templates-for-ri-md/mi-template.md`
- `recce-ui` → `package.json`

Rules: never edit `vendor/**` · a submodule bump needs a human (`pf vendor approve`) · `pf vendor drift` names the files to re-read.
