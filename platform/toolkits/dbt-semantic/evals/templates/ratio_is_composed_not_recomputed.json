{
  "template": "ratio_is_composed_not_recomputed",
  "agent": "metric_gap_proposer",
  "why": "Pins the composition rule this toolkit's skill states: prefer composing existing metrics over recomputing. A ratio recomputed as its own simple metric is the bug that produces two different conversion rates in one company, and it re-divides wrongly at every grain except the one it was written for — `avg(ratio)` instead of `sum(num)/sum(den)`. The proposal must also stay inside the columns the mart actually has.",
  "tags": ["metrics", "composition"],
  "requires": {"mart": "uncovered_mart"},
  "input": {
    "gaps": ["{{mart.name}}"],
    "mart_detail": "model:{{mart.name}} [grain: {{mart.grain}}]\nColumns: {{mart.columns}}\n\nNo metric currently references this mart. Where two of its measures stand in a part-to-whole relationship, a ratio metric composed from them is preferred to a new metric that recomputes the division in SQL."
  },
  "expect": {
    "proposals": {"min_count": 1},
    "proposals[].metric_name": {"matches": "^[a-z][a-z0-9_]*$"},
    "proposals[].measure_or_expr": {"references_only": "{{mart.identifiers}}"}
  }
}
