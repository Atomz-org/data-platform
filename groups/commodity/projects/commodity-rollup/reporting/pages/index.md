---
title: commodity-rollup — Overview
queries:
  - metrics/landed_usd_total.sql
  - metrics/landed_usd_days.sql
  - metrics/benchmark_usd_total_where_landed.sql
  - metrics/spread_usd_total.sql
  - metrics/cheapest_usd_total.sql
  - metrics/spread_days.sql
  - metrics/avg_landed_price_usd.sql
  - metrics/avg_import_parity_ratio.sql
  - metrics/avg_spread_to_cheapest_usd.sql
  - metrics/avg_spread_to_cheapest_ratio.sql
  - metrics/days_as_cheapest_market.sql
---

Every number on this page is a governed metric compiled from the dbt
semantic layer into `queries/metrics/`. Nothing here restates business
logic — if a figure you need is missing, the fix is a metric definition,
not SQL in this page.

```sql date_bounds
-- DateRange reads min()/max() from ONE column, so both bounds must
-- arrive as two rows in that column — not two columns of one row.
select min(metric_time) as metric_time from ${metrics_landed_usd_total}
union all
select max(metric_time) from ${metrics_landed_usd_total}
```

<DateRange name=period data={date_bounds} dates=metric_time/>

```sql kpi_landed_usd_total
select sum(landed_usd_total) as landed_usd_total
from ${metrics_landed_usd_total}
```

```sql kpi_landed_usd_days
select sum(landed_usd_days) as landed_usd_days
from ${metrics_landed_usd_days}
```

```sql kpi_benchmark_usd_total_where_landed
select sum(benchmark_usd_total_where_landed) as benchmark_usd_total_where_landed
from ${metrics_benchmark_usd_total_where_landed}
```

```sql kpi_spread_usd_total
select sum(spread_usd_total) as spread_usd_total
from ${metrics_spread_usd_total}
```

<Grid cols=4>

<BigValue data={kpi_landed_usd_total} value=landed_usd_total title='Landed USD Total (component)' fmt=num0/>
<BigValue data={kpi_landed_usd_days} value=landed_usd_days title='Days With a Comparable Landed Price' fmt=num0/>
<BigValue data={kpi_benchmark_usd_total_where_landed} value=benchmark_usd_total_where_landed title='Benchmark USD Total, landed days (component)' fmt=num0/>
<BigValue data={kpi_spread_usd_total} value=spread_usd_total title='Spread Total (component)' fmt=num0/>

</Grid>

## Landed USD Total (component) over time

```sql trend
select metric_time, sum(landed_usd_total) as landed_usd_total
from ${metrics_landed_usd_total}
group by 1 order by 1
```

<LineChart data={trend} x=metric_time y=landed_usd_total yFmt=num0/>

## Landed USD Total (component) by commodity id

```sql breakdown
select commodity_id, sum(landed_usd_total) as landed_usd_total
from ${metrics_landed_usd_total}
where commodity_id is not null
group by 1 order by 2 desc
```

<BarChart data={breakdown} x=commodity_id y=landed_usd_total swapXY=true xFmt=num0/>

## Detail

<DataTable data={breakdown} rows=15/>

---

_Generated by `pf report build`. Metric definitions live in
`transform/models/semantic/`; this page is a projection of them._
